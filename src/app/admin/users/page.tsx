import SoonStub from "../_components/SoonStub";

export default function Page() {
  return (
    <SoonStub
      title="Team"
      checkpoint="checkpoint 5.4"
      blurb="Manage DestaLabs team accounts — invite agents and toggle roles (AGENT / ADMIN)."
    />
  );
}
